steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - Dockerfile.address-places
      - --build-arg
      - CIRCUM_SOURCE_SHA=${_CIRCUM_SOURCE_SHA}
      - --tag
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
